services:
  stock-dashboard:
    build: .
    container_name: stock-dashboard
    restart: unless-stopped
    environment:
      - TUSHARE_TOKEN=${TUSHARE_TOKEN}
    ports:
      - "8000:8000"
    networks:
      - traefik-external

networks:
  traefik-external:
    external: true
